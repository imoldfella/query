// every entitity has values that span domains
// domains can be have various properties such as if they are stored as deltas or values. they support various operations such as ordering.

// entities may have more than one value per domain. (entity is a set of tuples)

typedef BranchId = int;

class Branch {}

class Domain {}
